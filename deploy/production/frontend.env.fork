# Fork-private 前端构建变量模板（StoryFlow 白标）
# 部署前：cp deploy/production/frontend.env.fork frontend/.env
# 之后 docker compose build 会把整个 frontend/ 拷进镜像，Vite 自动读取本文件。
#
# 留空任意一项可回退到上游默认值（详见 frontend/.env.example）。

VITE_BRAND_NAME=StoryFlow
VITE_BRAND_TAGLINE=AI 短剧创作工作台
VITE_BRAND_DESCRIPTION=AI 短剧创作工作台，与 AI 助手对话完成创作。
